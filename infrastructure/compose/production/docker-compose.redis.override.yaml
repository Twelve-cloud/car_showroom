version: '3.7'

services:
  redis:
    env_file:
      - ./env/production/.env.prod.redis
    volumes:
      - ./car_salon_activities/data/redis:/root/redis
    networks:
      back-prod:
        ipv4_address: 172.168.10.8