version: '3.7'

services:
  redis:
    env_file:
      - ./env/tests/.env.tests.redis
    volumes:
      - ./car_salon_activities/data/tests/redis:/root/redis
    networks:
      back-tests:
        ipv4_address: 182.168.10.7